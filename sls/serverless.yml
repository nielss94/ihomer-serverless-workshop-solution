service: crypto-exchange
useDotenv: true

frameworkVersion: '3'

provider:
    name: aws
    runtime: nodejs12.x
    lambdaHashingVersion: 20201221
    stage: dev
    region: ${env:REGION}
    environment:
        DYNAMO_URL: ${env:DYNAMO_URL}
        DYNAMO_TABLE: ${env:DYNAMO_TABLE}
        REGION: ${env:REGION}
        AWS_ACCESS_KEY_ID: ${env:AWS_ACCESS_KEY_ID}
        AWS_SECRET_ACCESS_KEY: ${env:AWS_SECRET_ACCESS_KEY}
        UNIQUE_ID: ${env:UNIQUE_ID}
    apiGateway:
        shouldStartNameWithService: true

functions:
    getWallet:
        handler: src/handlers/getWallet.handler
    listWallets:
        handler: src/handlers/listWallets.handler

custom:
    appSync:
        name: api
        authenticationType: API_KEY
        schema:
            - graphql/_query.graphql
            - graphql/types.graphql
        dataSources:
            -   type: AWS_LAMBDA
                name: getWallet
                config:
                    functionName: getWallet
            -   type: AWS_LAMBDA
                name: listWallets
                config:
                    functionName: listWallets
        mappingTemplates:
            -   dataSource: getWallet
                type: Query
                field: getWallet
                request: false
                response: false
            -   dataSource: listWallets
                type: Query
                field: listWallets
                request: false
                response: false

    appsync-simulator:
        dynamoDb:
            region: ${env:REGION}
            endpoint: http://localhost:8000
            accessKeyId: ${env:AWS_ACCESS_KEY_ID}
            secretAccessKey: ${env:AWS_SECRET_ACCESS_KEY}
        apiKey: da2-fakeApiId123456
        location: .build
        watch: false


package:
    patterns:
        - 'graphql/**'

plugins:
    - serverless-plugin-typescript
    - serverless-appsync-simulator
    - serverless-offline
